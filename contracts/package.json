{
  "name": "smart-contract",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@openzeppelin/contracts": "^4.6.0",
    "truffle": "^5.5.16"
  },
  "devDependencies": {
    "nodemon": "^2.0.16"
  },
  "scripts": {
    "test": "npx truffle test",
    "develop": "nodemon -e js,sol --exec 'npx truffle test'",
    "deploy": "npx truffle migrate --network testnet"
  }
}
